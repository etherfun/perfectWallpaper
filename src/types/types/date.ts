/**
 * Wallpaper Properties 类型定义 — 日期格式配置
 */

export interface DateFormatConfig {
    yearFormat: number;
    monthFormat: number;
    dayFormat: number;
    weekFormat: number;
    separator: number;
    order: number;
}
